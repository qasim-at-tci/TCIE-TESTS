---
os: linux
dist: focal
arch: amd64
language: python
python:
  - 3.11.2

env:
  global:
    POETRY_VERSION: 1.6.1
    POETRY_OPTS: "--no-ansi --no-interaction --no-plugins"

before_install: virtualenv --version
install:
  - make setup

script:
  - virtualenv --version
